<h1>New comment</h1>

<%= error_messages_for :comment %>

<% form_for(:comment, :url => comments_path(:recipe_id => @recipe)) do |f| %>
  <p>
    <%= text_area :comment, :comment %>
    <%= submit_tag "Create" %>
  </p>
<% end %>