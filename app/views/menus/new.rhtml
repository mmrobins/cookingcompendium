<h1>New Menu</h1>

<%= error_messages_for :menu %>

<% form_for(:menu, :url => menus_path) do |f| %>
  <p>
    <b>Name (ex Seasonal Dinner)</b><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', menus_path %>